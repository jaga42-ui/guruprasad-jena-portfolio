import ProjectsScreen from './screen';

export const metadata = {
  title: "Projects — Guruprasad Jena",
  description: "Four case files — Umbrix, GetFreeToolsAI, Veritas Picks and Sahayam — plotted as blueprint sheets.",
};

export default function Page() {
  return <ProjectsScreen />;
}
